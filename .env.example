VITE_API_BASE_URL=http://localhost:5054
# Base do API Gateway (LocalStack). O id da REST API muda a cada restart do
# LocalStack (sem persistência) — confira com `awslocal apigateway get-rest-apis`.
VITE_GATEWAY_BASE_URL=http://localhost:30466/restapis/<rest-api-id>/dev/_user_request_
